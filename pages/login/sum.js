import React from "react";

const styles = {
    display: 'flex',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: '50px',
    fontSize: 'xx-large',
    fontWeight: 'bolder',
    fontStyle: 'italic'
}
const sum = () => {
  return <div className="heading" style={styles}>Sum is great</div>
};

export default sum;
